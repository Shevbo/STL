# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.hypothesis/

# Env / secrets
.env
*.env
.shectory_trade.env

# Frontend
frontend/node_modules/
frontend/dist/
frontend/.svelte-kit/
frontend/mock/venv/
frontend/.vite/

# Deploy logs
*.log
deploy.log
test-results/

# Poetry
poetry.lock

# Proto stubs are committed (grpcio-tools needed to regenerate)

# Claude Code local settings (machine-specific)
.claude/settings.local.json

# Playwright MCP local state
.playwright-mcp/

# Debug screenshots
*.png

# Root npm lockfile (accidental)
/package-lock.json

# harness scratch (agent working notes)
.harness/notes/
*.tar.gz
*.jpeg

# Собирается из companion/*.go, не версионируется. Второй шаблон — про то, что
# `go build ./...` в этой папке кладёт рядом companion.exe по имени модуля:
# консольная сборка без -H=windowsgui, запускать её вместо панели незачем.
companion/STLCompanion.exe
companion/companion.exe

# Сессионные артефакты браузера и кэш graphify — не версионируются
.playwright-mcp/
graphify-out/cache/
graphify-out/20*/
