---
name: project-mail-hook
description: Почта окна переживает перезапуск сессии через хук mail_hook.py в settings.local.json (только показ, без автоответа); крон — лишь дополнение для простоя
metadata:
  type: project
---

17.09.2026, окно backtests. Разбор почты больше не держится на кроне сессии.

- **Хук:** `scripts/mail_hook.py` на SessionStart и UserPromptSubmit, прописан в `.claude/settings.local.json` окна (файл в .gitignore — на новой машине/клоне прописать заново): `python "$CLAUDE_PROJECT_DIR/scripts/mail_hook.py" backtests`. Ящик берёт через `devmsg.sh inbox-json`. Состояние (показанные id, время опроса) в `~/.stl-mailhook/backtests.json`.
- **Только показ.** Хук не отвечает, не делает ack, не запускает Claude. Причина — инцидент 18.08.2026 (коммит 438479e): мосты окон отвечали на подтверждения друг друга, 792 письма за ночь, лимит выжжен. Урок: участники, отвечающие на ответы друг друга, — генератор.
- **Правило «не отвечать на ответ»** вшито в вывод хука и в текст крона: письмо-ответ или подтверждение без новой просьбы — только ack.
- **Цена:** пустой ящик — молчит; ssh не чаще раза в 10 мин на ввод, на старте всегда; письмо целиком один раз, неподтверждённое — строкой раз в 30 мин; мёртвый транспорт печатает «ящик не проверен».
- **Крон** (CronCreate, каждый час в :07 с 7 до 22) остаётся только для простоя сессии и умирает при перезапуске; хук на SessionStart напоминает поставить его заново.
- CLAUDE.md устарел: `devmail_install.sh` и `devmail_hook.py` удалены ещё 18.08.

**Why:** крон сессии пропадал при перезапуске, а письмо real-trade пролежало 15 часов непрочитанным.
**How to apply:** после перезапуска почта придёт сама на первом вводе; крон поставить заново, если сессия будет простаивать. Никогда не добавлять автоответ на письма. См. [[project-dev-msg-middleware]].
